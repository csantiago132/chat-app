/**
 * Button component
 *
 */

import { styled } from "../../../../setup/theme";

const Button = styled.button`
  background: none;
  border: none;
  color: inherit;
  cursor: pointer;
  font: inherit;
  outline: inherit;
  padding: 0;
`;

export default Button;
