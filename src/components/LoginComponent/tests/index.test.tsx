/* import * as Enzyme from 'enzyme';
import * as React from 'react';
import * as enzymeAdapterReact16 from 'enzyme-adapter-react-16';
import LoginComponent from '../LoginComponent';
import * as googleAsset from '../../../assets/google_signin_buttons/web/2x/btn_google_signin_light_normal_web2x.png';

Enzyme.configure({ adapter: new enzymeAdapterReact16() });

const renderedComponent = Enzyme.shallow(<LoginComponent />);

describe('<LoginComponent />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });

  it('should render an alt tag', () => {
    expect(renderedComponent.prop('alt')).toEqual(googleAsset);
  });
});
 */
