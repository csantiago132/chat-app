import { Col } from "react-styled-flexboxgrid";
import { styled } from "../../../../setup/theme";

const AsideInfo = styled(Col)`
  background: ${(props) => props.theme.color.color_offwhite};
`;

export default AsideInfo;
